import { Stack } from "expo-router";

// Navegacion interna de categorias, controlada por la pantalla principal.
export default function CategoriasLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
