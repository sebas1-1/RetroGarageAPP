import { Stack } from "expo-router";

// Navegacion interna de citas: agenda, nueva cita y editar cita.
export default function CitasLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
