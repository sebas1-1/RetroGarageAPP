import { Stack } from "expo-router";

// Navegacion interna de usuarios: lista, nuevo usuario y editar usuario.
export default function UsuariosLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
